package ch.ethz.ivt.abmt.exercise3;

import java.io.IOException;

public class Exercise3_3 {
	public static void main( String[] args ) {
		// write code that calls performIO() 30 times,
		// and prints "i: result" when IO succeeds,
		// with i the iteration number and result the output of perform IO.
		// After printing, you should throw a RuntimeException if the result is negative
		// If an IOException occurred, you should print a message stating so.
		// If a RuntimeException was raised, you should print a message stating so

	}
}
